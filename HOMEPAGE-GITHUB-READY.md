# IB-TECH ePINs — New Homepage

This package contains the prepared source for the new `/` homepage based on the approved IB-TECH ePINs design.

## Changed
- Replaced only `index.html` with the new landing page.
- Added the dark blue/gold responsive hero design.
- Added IB-TECH ePINs logo to the header and phone mockup.
- Added MTN, Glo, Airtel and 9mobile cards only at the bottom of the homepage.
- Removed the previous homepage feature/contact/network sections from the root page.
- Kept existing login, signup, dashboard, admin, wallet, transactions and server files unchanged.
- Added mobile navigation and responsive layouts.

## GitHub
Copy the contents of `IB-TECH-ePINs-final-site/` into the existing GitHub repository and replace the existing `index.html` and `css/style.css` with these versions.

Do not delete the existing `server/`, `js/`, `images/`, authentication pages, dashboard pages or admin pages.

Railway should redeploy the repository after the changes are committed to `main`.
